spring:
  application:
    name: 'transaction-service'
  profiles:
    active: ${APP_PROFILE}
  cloud:
    config:
      uri: http://localhost:8888
      username: ${CONFIG_USER}
      password: ${CONFIG_PASSWORD}