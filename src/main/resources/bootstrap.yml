spring:
  application:
    name: 'transaction-service'
  profiles:
    active: ${APP_PROFILE}
  cloud:
    config:
      uri: ${CONFIG_SERVICE_URL}
      username: ${CONFIG_USER}
      password: ${CONFIG_PASSWORD}